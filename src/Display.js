import React from 'react';
import './Display.css';

// This is the display component where the calculation and the input from the keypad is displayed.

const display = (props) => {
    
    return (
        <div className="Display">
            <p>Display area.</p>
        </div>
    );
};

export default display;